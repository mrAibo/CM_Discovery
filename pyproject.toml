[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ibm-patchwatch"
version = "0.5.0"
description = "IBM metadata providers for the portable Patchwatch catalog"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "CM Discovery contributors"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
