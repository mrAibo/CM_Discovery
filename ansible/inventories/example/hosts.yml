all:
  children:
    ibm_was:
      hosts:
        cmtest:
          ansible_host: cmtest.example.invalid
          ansible_user: UNRESOLVED
          # An vorhandene Python-3.12-Installation anpassen.
          ansible_python_interpreter: /usr/bin/python3.12
